<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">

    <title>File Uploader</title>

    <!-- CSS -->
    <link rel="stylesheet" type="text/css" href="styles/index.css">

    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css"
        integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" crossorigin="anonymous">

    <!-- Scripts -->
    <script src="js/jquery.slim.min.js"></script>
    <script src="js/popper.min.js"></script>
    <script src="js/bootstrap.min.js"></script>
    <script src="https://kit.fontawesome.com/437a6927d3.js" crossorigin="anonymous"></script>

    <script>
        function clear(){
            document.getElementById("uploadForm").reset();
        }
    </script>
</head>

<body>

    <nav class="navbar fixed-top navbar-expand-lg navbar-transparent">
        <ul class="navbar-nav ml-md-auto">
            <li class="nav-item dropdown">
                <div class="nav-link dropdown-toggle text-light" id="profile" data-toggle="dropdown"
                    style="cursor: pointer;">
                    <img src="<%=picture%>" width="30" height="30" alt="profile-pic" class="rounded-circle" />
                    <span class="username"><%=username%></span>
                </div>
                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="profile">
                    <a class="dropdown-item" href="/logout">Logout</a>
                </div>
            </li>
        </ul>
    </nav>

    <div class="theme">

        <div class="shape-circle"></div>

        <div class="container-fluid">

            <div class="row">

                <div class="col-6 mt-5 p-0">
                    <div class="d-flex justify-content-between">
                        <img src="/images/drive.png" class="d-none d-lg-block wh-32 mr-3 mt-2">
                        <div>
                            <h1 class="text-title">GOOGLE DRIVE</h1>
                            <p class="text-para ml-2">File sharing platform</p>
                        </div>
                    </div>
                </div>

                <div class="col-6 content-right">

                   
                    <div class="d-flex justify-content-center">
                        <div class="col-md-8">
                                
                            <h1 class="text-center ml-3">UPLOAD FILE HERE!!!</h1>
                            <form id="uploadForm" action="/upload" method="POST" enctype="multipart/form-data">

                            <div class="form-group mt-3">
                                <input type="file" class="form-control p-1" name="file" required />
                            </div>


                            <% if (success) { %>
                                <script>
                                    var test = '<%- JSON.stringify(success) %>';
                                    var width = 10;
                                    if (test) {
                                        var id = setInterval(progress, 10);
                                        function progress() {
                                            if (width >= 100) {
                                                clearInterval(id);
                                            } else {
                                                width++;
                                                document.getElementById("progressStatus").style.width = width + '%';
                                            }
                                        }
                                    }
            
                                </script>
                                <div style="margin: auto;">
                                    <div id="progressBar">
                                        <div id="progressStatus"></div>
                                    </div>
                                </div>
                                <%}%>
           
                            <div class="d-flex justify-content-around mt-4">
                                <button type="button" onclick="clear()" class="btn btn-danger layout">
                                    <a href="/" style="text-decoration: none;color: white;">
                                        Clear
                                    </a>
                                </button>   
                                <button class="btn btn-outline-light layout signin">
                                    Upload File
                                </button>   
                            </div>

                            </form>

                        
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

</body>

</html>